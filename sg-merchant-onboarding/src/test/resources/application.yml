spring:
  datasource:
    url: jdbc:h2:mem:onboarding;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  liquibase:
    enabled: false

merchant-onboarding:
  acquiring:
    enabled: false

jwt:
  secret: ScenarioGeneratorTestSecretKey2026ScenarioGeneratorTestSecretKey2026
  expiration-ms: 3600000
