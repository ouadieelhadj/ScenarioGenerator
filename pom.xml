<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
    </parent>

    <groupId>com.staging</groupId>
    <artifactId>scenario-generator</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>ScenarioGenerator</name>
    <description>Mastercard Simulator — Multi-Module</description>

    <modules>
        <module>sg-common</module>
        <module>sg-generator-orchestrator</module>
        <module>sg-dmcs-issuer</module>
        <module>sg-dmcs-acquirer</module>
        <module>sg-dmcs-common</module>
        <module>sg-mc-dmas-member</module>
        <module>sg-mc-dmas-mastercard</module>
        <module>sg-swam-issuer</module>
        <module>sg-swam-acquirer</module>
        <module>sg-swam-lis-common</module>
        <module>sg-swam-lis-member</module>
        <module>sg-swam-lis-switch</module>
        <module>sg-mc-sms-acquirer</module>
        <module>sg-mc-sms-issuer</module>
        <module>sg-card-issuing</module>
        <module>sg-acquiring</module>
        <module>sg-way-pos-server</module>
        <module>sg-way-pos-simulator</module>
        <module>sg-merchant-site-simulator</module>
        <module>sg-3ds-member</module>
        <module>sg-3ds-network-simulator</module>
        <module>sg-visa-mastercard-gateway-simulator</module>
        <module>sg-issuing-internal-e2e</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <!-- Mockito/Byte Buddy récents requis pour exécuter les tests sous JDK 26. -->
        <mockito.version>5.23.0</mockito.version>
        <byte-buddy.version>1.18.7</byte-buddy.version>
        <jpos.version>2.1.9</jpos.version>
        <jjwt.version>0.11.5</jjwt.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- sg-common -->
            <dependency>
                <groupId>com.staging</groupId>
                <artifactId>sg-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.staging</groupId>
                <artifactId>sg-swam-lis-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.staging</groupId>
                <artifactId>sg-dmcs-common</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- jPOS -->
            <dependency>
                <groupId>org.jpos</groupId>
                <artifactId>jpos</artifactId>
                <version>${jpos.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
            </dependency>

            <!-- Apache POI -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>5.2.5</version>
            </dependency>

            <!-- OpenPDF -->
            <dependency>
                <groupId>com.github.librepdf</groupId>
                <artifactId>openpdf</artifactId>
                <version>1.3.30</version>
            </dependency>

            <!-- JFreeChart -->
            <dependency>
                <groupId>org.jfree</groupId>
                <artifactId>jfreechart</artifactId>
                <version>1.5.4</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>21</source>
                        <target>21</target>
                        <compilerArgument>-proc:none</compilerArgument>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
