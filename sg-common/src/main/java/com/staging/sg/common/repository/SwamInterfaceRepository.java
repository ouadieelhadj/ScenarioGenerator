package com.staging.sg.common.repository;

import com.staging.sg.common.entity.SwamInterface;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SwamInterfaceRepository extends JpaRepository<SwamInterface, String> {
}
